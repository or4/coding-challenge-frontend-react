import styled from 'styled-components';

export const Container = styled.div`
    min-height: 40px;
    line-height: 40px;
    text-align: right;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
`;
